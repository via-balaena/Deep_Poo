# Vision module

Contains detector interfaces, capture types, and overlay helpers.
- `interfaces.rs`: Detector and Frame definitions.
- `overlay.rs`: box normalization/drawing helpers (used by HUD, single_infer, overlay_labels).
- `vision_core.rs`: front camera capture and inference systems.
