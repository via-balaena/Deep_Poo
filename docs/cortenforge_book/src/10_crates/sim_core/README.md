# sim_core

**Why**: The simulation scaffold that everything else plugs into.
**How it fits**: Owns the runtime loop, modes, and hooks for capture/inference.
**Learn more**: Use the pages below; docs.rs: https://docs.rs/cortenforge-sim-core.

